# 検証記録

2026-09-08 / Windows x64 / .NET SDK 10.0.400

- ソリューションのビルド成功（警告・エラーなし）。
- xUnit: 17件成功。枠変換、未知値、64ビット値、SQLite保存、通知永続化、Claude並列セッション、設定復元、障害分離を検証。
- 配布版を使用して Codex・Copilot の実アカウントの残り枠を取得。Codex の日別トークン履歴も取得。
- 自己完結型配布版のWPF画面をPNGへ描画し、ダーク配色と3サービスの同時表示を確認。
- 配布版の起動、使用量取得、データ保存、正常終了を確認。
- `scripts/verify-bridge.ps1`: 収集プログラムを実プロセスとして起動し、標準入力・標準出力の完全一致、日本語、残り率変換、会話本文の保存除外を確認。
- 実ユーザーの Claude ステータスラインをバックアップして連携を登録。既存 `oh-my-posh claude` は転送先として保持。

Claudeの実セッションからの初回受信は、ユーザーの次回利用時に確認する。検証のための生成リクエストは送っていない。物理的なモニター交換・スリープ操作・通知の実表示は自動検証していない。

再検証:

```powershell
dotnet test AiUsageWidget.slnx
.\publish.ps1
.\scripts\verify-bridge.ps1
```
