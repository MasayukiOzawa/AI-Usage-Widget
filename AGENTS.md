# リポジトリの作業指示

## ブランチ運用

- 新しい変更作業を開始する際は、現在のブランチと未コミット変更を確認する。既存の変更を保護したうえで、`origin/main` の最新情報を取得し、ローカルの `main` を更新する。
- 新しい変更作業では、ファイルを変更する前に、最新の `main` から新しい作業用ブランチを作成する。ユーザーから別のブランチ名を指定されていない場合は、`codex/<作業内容を表す短い名前>` の形式を使用する。
- `main` へ直接コミットしない。
- 1つのブランチでは、1つの論理的な作業だけを扱う。同じ作業を継続・再開する場合は、既存の作業用ブランチを使用し、変更のたびに `main` の更新やブランチの作り直しを行わない。
- ブランチがマージされた後は `main` に戻り、最新状態へ更新してから、マージ済みのローカル作業用ブランチを削除する。
- リポジトリのファイルを変更しない読み取り専用の調査では、新しいブランチを作成する必要はない。

## PR作成の許可

- PRを作成する前に、変更内容の確認と必要な検証を完了し、対象ブランチ、マージ先、タイトル案、説明案、検証結果をユーザーへ提示して、作成の許可を求める。ドラフトPRも対象とする。
- ユーザーから明示的な許可を得るまでPRを作成しない。実装、コミット、プッシュへの許可だけでは、PR作成への許可とみなさない。
- ユーザーが当該PRの作成を明示的に依頼または許可している場合は、同じ範囲で再確認する必要はない。

## 既存変更の保護

- ユーザーや他の作業による未コミット変更・未追跡ファイルを、許可なく破棄、上書き、削除しない。ブランチ切り替えや競合解消の際も保護する。
- コミット前に差分を確認し、今回の作業に関係する変更だけを含める。既存変更と分離できない場合は、変更内容を示してユーザーへ確認する。

## 検証と報告

- 変更内容と影響範囲に応じて、ビルド、テスト、必要な実動作確認を行う。文書のみの変更は、内容と差分の確認を基本とする。
- 検証に失敗した場合は原因を調べ、必要な修正を行う。未実施・実施不能の検証は、成功した検証と区別して理由を報告する。
- 完了報告には、変更内容、検証結果、残っている制約や未対応事項を記載する。実施していない検証を成功と報告しない。

## 配布物の更新

- ソースコードを変更してビルドする作業では、必要なテストの成功後に `./publish.ps1` を実行し、`artifacts/win-x64/AiUsageWidget.exe` と必要な関連ファイルを生成・更新する。文書だけを変更する作業では、この発行は不要とする。
- `artifacts` へ発行する前に、同じ出力先のアプリが起動中であれば終了し、ファイルを正常に更新できる状態にする。
- ソースのビルドと `artifacts` への配布用バイナリの発行を区別し、どちらを実施したか報告する。ビルド成功だけで配布物を更新済みと扱わない。
- 配布物を更新した場合は、出力先と、起動中のアプリを再起動して新版へ切り替えたかを報告する。
- `artifacts` などの配布フォルダーや実行可能プログラムはGitへコミットしない。公開用の実行可能プログラムはGitHub Releasesで配布する。

## 秘密情報と実データの保護

- アクセストークン、リフレッシュトークン、資格情報ファイルなどの秘密情報を、画面、コマンドライン、ログ、コミット、PRへ出力・添付しない。
- 実ユーザーの `history.db`、設定ファイル、診断データをGitへコミットしない。公開する文書や画像にも、秘密情報や不要な個人情報が含まれていないことを確認する。
- 実データベースの修復、移行、初期化、手動更新を行う前に、アプリの書き込みを停止するなどして整合性のあるバックアップを作成し、復元できるようにする。SQLiteのWALなどの関連ファイルも考慮する。この事前バックアップは、アプリの通常の履歴記録ごとには要求しない。

## マージと公開操作の許可

- PRのマージ、タグのプッシュ、GitHub Releaseの作成・公開・公開済み配布物の差し替えは、対象と実施内容を示してユーザーから明示的な許可を得てから行う。
- 実装、コミット、作業ブランチのプッシュ、PR作成への許可を、マージやRelease公開への許可とみなさない。Release作成を起動するタグのプッシュも公開操作として扱う。
- ユーザーが対象の操作を明示的に依頼または許可している場合は、同じ範囲で再確認する必要はない。
